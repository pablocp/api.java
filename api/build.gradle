group = rootProject.group
version = rootProject.version

dependencies {
	compile lib.launchpad_sdk

	testCompile lib.junit
	testCompile lib.launchpad_serializer_json_jodd
	testCompile project(':api-transport-jodd')
}

jar.version = version